server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # Isso faz o Nginx redirecionar todas as rotas para o React
    location / {
        try_files $uri $uri/ /index.html;
    }
}
